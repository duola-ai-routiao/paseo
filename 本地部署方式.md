# 本地部署方式

> 整理日期：2026-08-05
> 环境：本机 `/home/alan/paseo` 仓库，Node v24.18.0（nvm）

## 一、本机 6767 端口现状

| 项目     | 详情                                                            |
| -------- | --------------------------------------------------------------- |
| 服务     | Paseo Daemon（agent 生命周期管理 + WebSocket API + MCP server） |
| 进程     | `Paseo Daemon`（父进程 `Paseo Supervisor`，负责守护与崩溃重启） |
| 运行时   | `/home/alan/.nvm/versions/node/v24.18.0/bin/node`               |
| 代码来源 | 直接运行 `/home/alan/paseo` 仓库工作区代码（非 npm 全局安装）   |
| 监听地址 | `127.0.0.1:6767`（仅本机回环，不暴露局域网/公网）               |
| 数据目录 | `PASEO_HOME=/home/alan/.paseo`（生产 home）                     |
| 健康检查 | `curl http://127.0.0.1:6767/api/health` → `{"status":"ok"}`     |

### 同机其他 Paseo 相关实例

- **6769 端口**：另一个 Paseo Daemon，同样由 Supervisor 拉起、跑本仓库代码。
- **Docker 容器 `paseo`**：容器化实例，容器内 6767 映射到宿主 **8234** 端口（`unless-stopped` 重启策略），与宿主机 6767 互不冲突。

## 二、Supervisor 的周期机制

Supervisor 本身**不做远程同步**，只有两个纯本地计时器：

| 计时器                       | 周期     | 用途                                       |
| ---------------------------- | -------- | ------------------------------------------ |
| Supervisor → Worker IPC 心跳 | 每 1 秒  | 确认 daemon worker 子进程活着              |
| PID 锁心跳                   | 每 30 秒 | 刷新 `$PASEO_HOME` 下 pid 锁，防多实例冲突 |

远程访问由 daemon 进程通过 **relay 长连接**（E2E 加密 WebSocket）实现，事件实时推送，无固定同步周期。

## 三、标准部署方式（推荐）

不需要 systemd / Docker / pm2 —— Paseo 自带 Supervisor 守护机制。

### 1. 安装

```bash
npm install -g @getpaseo/server @getpaseo/cli
```

### 2. 启动生产 daemon（默认 6767 端口）

```bash
paseo daemon start
```

行为说明：

- 默认端口 **6767**，默认 home `~/.paseo`
- 以 detached 后台进程拉起 **Supervisor**，Supervisor 再 fork daemon worker
- 日志写入 `~/.paseo/daemon.log`（自动轮转）
- PID 锁 + 30 秒心跳，防多实例冲突
- worker 崩溃（非零退出 / 非 SIGTERM）自动拉起

### 3. 常用管理命令

```bash
paseo daemon status                 # 查看状态
paseo daemon restart                # 重启
paseo daemon stop                   # 停止
paseo daemon start --web-ui         # 开启内置 Web UI（http://localhost:6767/）
paseo daemon start --no-relay       # 禁用 relay（仅本机访问）
paseo daemon start --port 6768      # 换端口
paseo daemon start --foreground     # 前台调试模式
```

## 四、关键设计要点

1. **6767 是生产端口约定**：桌面端、手机 App、CLI 默认都连这个端口；开发用 `npm run dev`（端口 6768，`.dev/paseo-home`），避免与生产实例冲突。
2. **只监听 127.0.0.1**：远程访问走 relay（E2E 加密），不直接开放端口，这是安全模型要求（见 SECURITY.md）。
3. **崩溃自愈**：Supervisor 监听 worker 退出码，异常退出自动拉起，无需 systemd `Restart=always`。
4. **不要手动 `node dist/server/index.js` 裸跑**：那样没有 Supervisor、日志轮转、PID 锁，崩溃不会被托管。
5. **切勿随意重启 6767 端口的 daemon**——它管理着所有运行中的 agent（包括正在执行任务的 AI agent 自身）。

## 五、可选：开机自启（user-level systemd）

官方无内置 systemd unit，如需开机启动：

```ini
# ~/.config/systemd/user/paseo.service
[Unit]
Description=Paseo Daemon
After=network-online.target

[Service]
ExecStart=%h/.nvm/versions/node/v24.18.0/bin/paseo daemon start --foreground
Restart=on-failure

[Install]
WantedBy=default.target
```

```bash
systemctl --user daemon-reload && systemctl --user enable --now paseo
loginctl enable-linger $USER   # 未登录也保持运行
```

注意必须加 `--foreground`：让 systemd 直接管理前台进程，避免 detached 模式下 systemd 误判进程已退出。

## 六、代码来源：仓库代码直跑，而非 npm 全局安装

正在运行的 6767 daemon **使用的是本仓库工作区代码**，不是 `npm install -g` 安装的版本：

| 证据         | 内容                                                                                                                                         |
| ------------ | -------------------------------------------------------------------------------------------------------------------------------------------- |
| 运行版本     | daemon 日志自报 `daemonVersion: 0.2.0`，与仓库 `packages/cli/package.json` 一致                                                              |
| 全局安装版本 | npm 全局装的是 `@getpaseo/cli@0.2.3`（2026-07-29 安装），**版本对不上，处于闲置状态**                                                        |
| 进程环境     | `npm_lifecycle_event=npx`、`npm_lifecycle_script=tsx`、`INIT_CWD=/home/alan/paseo` —— 在仓库目录用 **npx + tsx 直接跑源码** 启动             |
| 启动者       | 由 **ginit 编排**拉起（`GINIT_PASEO_PATH=/home/alan/paseo/packages/cli/bin/paseo`、`GINIT_PASEO_HUB_WS_PORT=8235`），父进程是 systemd --user |

结论：`npm i -g` 装的那份 0.2.3 **存在但没被使用**；真正干活的 6767 是仓库代码直跑。

## 七、本分支对运行代码的修改情况

本分支 `feat_ginit_connect_20260730` 相对 `main` 有 **171 个文件、+12459/-1432 行**差异。

**未改动的部分**：Supervisor/daemon 启动链路（`supervisor.ts`、`supervisor-entrypoint.ts`、`daemon-worker.ts`、`packages/cli/src/commands/daemon/`）相对 main 零改动——守护机制、1 秒 IPC 心跳、30 秒 PID 锁均为官方原版行为。

**已改动的部分**（ginit 接入功能，已包含在运行中的 daemon 里）：

- `feat(ginit)` 系列：Feishu host discovery、ginit connect 相关修复
- `feat(hub)`：relay E2EE 密钥与 hub 签名密钥分离、execution session 恢复
- `scripts/relay-server.mjs`（+353 行，新增）
- app 端 workspace / command-center / e2e 测试等改动
- QW / summery / docs 文档

**影响**：因为 daemon 用 tsx 直接跑工作区源码，当前 checkout 分支的所有改动都在 6767 daemon 中实时生效。若要切回官方纯净版，需 `paseo daemon stop` 后用全局 CLI（0.2.3）启动，而非让 ginit 从仓库拉起。

## 八、具体部署命令（两条路线）

### 路线 A：复现当前部署（仓库代码直跑，ginit 同款）⭐推荐

与现在 6767 跑的方式完全一致——直接用仓库代码启动：

```bash
cd /home/alan/paseo

# 1. 确保依赖和构建产物是最新的
npm install
npm run build:server

# 2. 启动 daemon（后台模式，默认 6767 端口、~/.paseo 数据目录）
PASEO_HOME=/home/alan/.paseo \
PASEO_LISTEN=127.0.0.1:6767 \
npx tsx packages/server/scripts/supervisor-entrypoint.ts
```

**用 ginit 的 CLI 包装方式启动（和现在一模一样）**：

```bash
cd /home/alan/paseo
PASEO_HOME=/home/alan/.paseo ./packages/cli/bin/paseo daemon start
```

> 注意：`packages/cli/bin/paseo` 指向 `dist/index.js`，所以第 1 步的 `npm run build:server` 必须先跑。

**验证**：

```bash
curl http://127.0.0.1:6767/api/health
# 期望输出: {"status":"ok","timestamp":"..."}
```

### 路线 B：官方标准部署（npm 全局安装）

如果想用官方发布版（就是已闲置的 0.2.3 那份），先停掉现在的：

```bash
# 1. 停掉当前仓库代码跑的 daemon
cd /home/alan/paseo
./packages/cli/bin/paseo daemon stop
# 或： kill <Supervisor PID>（会带着 worker 一起退）

# 2. 用全局 CLI 启动（0.2.3 官方版）
paseo daemon start

# 3. 验证
paseo daemon status
curl http://127.0.0.1:6767/api/health
```

升级全局版本时：

```bash
npm install -g @getpaseo/server@latest @getpaseo/cli@latest
paseo daemon restart
```

### 两条路线的区别

|          | 路线 A（仓库直跑）                             | 路线 B（全局安装）                       |
| -------- | ---------------------------------------------- | ---------------------------------------- |
| 代码内容 | **当前分支的所有改动都生效**（ginit/hub 功能） | 官方 0.2.3 纯净版，**没有 ginit 改动**   |
| 更新方式 | `git pull` + `npm run build:server` + 重启     | `npm i -g ...@latest` + `daemon restart` |
| 适合场景 | 正在开发 ginit 接入，需要改动实时生效          | 只想要稳定的官方功能                     |

**⚠️ 关键提醒**：当前分支改动（ginit 连接、hub 密钥分离等）只有在路线 A 下才生效；切到路线 B 后 ginit 相关功能会消失。且 6767 的 daemon 正在管理运行中的 agent，**stop/restart 之前确认没有重要任务在跑**。

## 九、总结

`npm i -g` 装好后执行 `paseo daemon start`，6767 端口即按官方形态部署完成：Supervisor 守护、日志轮转、PID 锁、relay 加密远程访问全部自带，无需第三方进程管理器。
